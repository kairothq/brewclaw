---
phase: 20-dashboard-redesign
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - components/dashboard/ChatArea.tsx
  - components/dashboard/ChatInput.tsx
  - components/dashboard/MessageBubble.tsx
  - app/(product)/dashboard/page.tsx
autonomous: true
requirements:
  - DASH-07

must_haves:
  truths:
    - "User sees conversation messages in the main area when a thread is selected"
    - "User can type a message in the 'Ask anything' input and send it"
    - "Sent message appears immediately in the thread message list"
    - "User sees conversation history with pinned threads and search results in sidebar"
    - "User can start a new conversation from the empty state"
  artifacts:
    - path: "components/dashboard/ChatArea.tsx"
      provides: "Message list display with auto-scroll and empty state"
      min_lines: 80
    - path: "components/dashboard/ChatInput.tsx"
      provides: "Chat input bar with send button and attachment placeholder"
      min_lines: 50
    - path: "components/dashboard/MessageBubble.tsx"
      provides: "Individual message rendering (user vs assistant styling)"
      min_lines: 30
    - path: "app/(product)/dashboard/page.tsx"
      provides: "Composed chat view using ChatArea and ChatInput"
      min_lines: 40
  key_links:
    - from: "components/dashboard/ChatArea.tsx"
      to: "lib/dashboard-store.ts"
      via: "useDashboardStore + selectActiveThread"
      pattern: "selectActiveThread"
    - from: "components/dashboard/ChatInput.tsx"
      to: "lib/dashboard-store.ts"
      via: "addMessage action"
      pattern: "addMessage"
    - from: "app/(product)/dashboard/page.tsx"
      to: "components/dashboard/ChatArea.tsx"
      via: "import and compose"
      pattern: "ChatArea"
---

<objective>
Build the chat interface — the primary interaction area of the redesigned dashboard. Users see their conversation messages and can type new ones.

Purpose: The chat interface is the core of the ClawNow.ai-inspired redesign. It replaces the old metrics grid as the main dashboard content, making the product feel like a conversational AI tool.
Output: ChatArea, ChatInput, MessageBubble components wired to the Zustand dashboard store.
</objective>

<execution_context>
@/Users/divykairoth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/divykairoth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-dashboard-redesign/20-01-SUMMARY.md
@lib/dashboard-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MessageBubble and ChatArea components</name>
  <files>
    components/dashboard/MessageBubble.tsx
    components/dashboard/ChatArea.tsx
  </files>
  <action>
**MessageBubble.tsx** — Renders a single chat message:
- Props: `message: Message` (from dashboard store type), `isUser: boolean`
- User messages: right-aligned, bg-orange-500/10 border border-orange-500/20 rounded-2xl rounded-br-sm, text-white
- Assistant messages: left-aligned, bg-zinc-800 border border-zinc-700 rounded-2xl rounded-bl-sm, text-zinc-100
- Show timestamp below message in text-xs text-zinc-500 format (e.g., "2:34 PM")
- Wrap in `motion.div` with initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} for entrance animation
- Max width: max-w-[80%] on desktop, max-w-[90%] on mobile

**ChatArea.tsx** — Main message display area:
- Uses `useDashboardStore` with `selectActiveThread` selector to get current thread messages
- Renders messages using MessageBubble component
- Auto-scrolls to bottom on new messages using `useRef` + `useEffect` with `scrollIntoView({ behavior: 'smooth' })`
- **Empty state (no active thread):** Center the BrewClaw logo mark (large orange "B" in a circle, like the loading state in layout.tsx), below it "How can I help you today?" in text-xl text-zinc-400, and below that a subtitle "Select a conversation or start a new one" in text-sm text-zinc-500
- **Empty thread (active but no messages):** Same empty state but with "Start typing to begin your conversation" subtitle
- Full height flex column with overflow-y-auto, padding p-4 sm:p-6, scrollbar styled with zinc colors
- Add a subtle gradient fade at the top (bg-gradient-to-b from-zinc-950 to-transparent h-8 sticky top-0) so messages scroll under it cleanly
  </action>
  <verify>TypeScript compiles: `npx tsc --noEmit`. Components render without errors when imported.</verify>
  <done>MessageBubble renders user/assistant messages with correct alignment and styling. ChatArea displays messages from active thread with auto-scroll, or shows empty state when no thread is selected.</done>
</task>

<task type="auto">
  <name>Task 2: Create ChatInput and wire up dashboard page</name>
  <files>
    components/dashboard/ChatInput.tsx
    app/(product)/dashboard/page.tsx
  </files>
  <action>
**ChatInput.tsx** — Chat input bar at the bottom of the main area:
- Sticky bottom bar: bg-zinc-950 border-t border-zinc-800, px-4 py-3
- Inner container: bg-zinc-900 border border-zinc-800 rounded-2xl flex items-end gap-2 p-2
- Left side: Attachment button (placeholder) using `Paperclip` icon from lucide-react, text-zinc-500 hover:text-zinc-300
- Center: `<textarea>` (not input — supports multiline) with:
  - placeholder="Ask anything..."
  - Auto-resize: min 1 row, max 5 rows. Use `useRef` + `useEffect` to adjust height on input: `textarea.style.height = 'auto'; textarea.style.height = textarea.scrollHeight + 'px'`
  - bg-transparent, no border, text-white, placeholder-zinc-500, resize-none, focus:outline-none
  - Submit on Enter (without Shift). Shift+Enter = newline.
- Right side: Send button using `ArrowUp` icon in a circle (w-8 h-8 rounded-full). When input is empty: bg-zinc-700 text-zinc-500 (disabled). When input has text: bg-orange-500 text-white (enabled). Transition between states.
- On submit: Call `addMessage(activeThreadId, { role: 'user', content: text })` from dashboard store. If no active thread exists, call `createThread()` first, then add message to the new thread. Clear input after send.
- Right side also: Regenerate button using `RotateCw` icon from lucide-react, text-zinc-500 hover:text-zinc-300 (placeholder, console.log on click)

**page.tsx** — Compose the full chat view:
- Replace the stub from Plan 01 with: `<div className="flex-1 flex flex-col min-h-0">` containing `<ChatArea />` (flex-1 overflow-y-auto) and `<ChatInput />` (sticky bottom)
- The page should fill the remaining space after the sidebar (the layout handles sidebar placement)
- Keep 'use client' directive
- Import ChatArea and ChatInput
- The page is a simple composition — all logic lives in components and the store
  </action>
  <verify>
Run `npm run build` — compiles without errors. Navigate to `/dashboard`:
1. See empty state with BrewClaw logo and "How can I help you today?"
2. Type a message and press Enter — thread is auto-created in sidebar, message appears in chat area
3. Type another message — it appears below the first, chat auto-scrolls
4. Click "+ New" in sidebar, type in the new thread — separate conversation
5. Switch between threads — messages change
  </verify>
  <done>
Full chat interface works: user can type messages, they appear in the chat area with correct styling (right-aligned user messages), threads are created automatically on first message, switching threads shows different conversations. Input supports multiline with Shift+Enter and auto-resizes.
  </done>
</task>

</tasks>

<verification>
1. Empty dashboard shows BrewClaw logo and "How can I help you today?"
2. Typing a message and pressing Enter creates a thread and shows the message
3. Messages appear with correct styling (user = right/orange, assistant = left/zinc)
4. Chat auto-scrolls to newest message
5. Input auto-resizes for multiline text
6. Shift+Enter creates newline, Enter sends
7. Switching threads in sidebar changes displayed messages
8. No TypeScript compilation errors
</verification>

<success_criteria>
- Chat interface renders as the main dashboard content
- Messages can be sent and appear in the conversation
- Thread auto-creation works when sending first message
- Thread switching displays correct messages
- Input UX works (auto-resize, Enter to send, Shift+Enter for newline)
</success_criteria>

<output>
After completion, create `.planning/phases/20-dashboard-redesign/20-02-SUMMARY.md`
</output>
