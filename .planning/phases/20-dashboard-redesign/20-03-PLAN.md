---
phase: 20-dashboard-redesign
plan: 03
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - components/dashboard/CreditsPanel.tsx
  - components/dashboard/SettingsModal.tsx
  - components/dashboard/ProviderCard.tsx
  - app/(product)/dashboard/layout.tsx
autonomous: true
requirements:
  - DASH-08
  - DASH-09
  - DASH-10
  - DASH-11

must_haves:
  truths:
    - "User can view remaining credits balance with daily and monthly spend breakdown"
    - "User can enter a dollar amount and initiate top-up from the credits panel"
    - "User can open settings modal and see tabs for Models, Providers, Channels, Skills"
    - "User can see a grid of AI providers with connect/disconnect buttons"
    - "Connected providers show a green checkmark indicator"
    - "Credits panel slides out from the sidebar when credits row is clicked"
  artifacts:
    - path: "components/dashboard/CreditsPanel.tsx"
      provides: "Slide-out credits panel with balance, spend breakdown, top-up input, auto-reload toggle"
      min_lines: 100
    - path: "components/dashboard/SettingsModal.tsx"
      provides: "Full-screen overlay modal with tab navigation (Models, Providers, Channels, Skills)"
      min_lines: 100
    - path: "components/dashboard/ProviderCard.tsx"
      provides: "Individual AI provider card with icon, name, description, connect/edit button"
      min_lines: 40
    - path: "app/(product)/dashboard/layout.tsx"
      provides: "Updated layout rendering CreditsPanel and SettingsModal overlays"
      min_lines: 60
  key_links:
    - from: "components/dashboard/CreditsPanel.tsx"
      to: "lib/dashboard-store.ts"
      via: "useDashboardStore creditsOpen + toggleCredits"
      pattern: "toggleCredits"
    - from: "components/dashboard/SettingsModal.tsx"
      to: "lib/dashboard-store.ts"
      via: "useDashboardStore settingsOpen + closeSettings + settingsTab"
      pattern: "closeSettings"
    - from: "app/(product)/dashboard/layout.tsx"
      to: "components/dashboard/CreditsPanel.tsx"
      via: "conditional render based on creditsOpen"
      pattern: "CreditsPanel"
---

<objective>
Build the Credits slide-out panel and Settings modal with AI provider management. These are the two overlay UIs that complete the dashboard's feature set.

Purpose: Users need to manage their credits (view balance, top up) and AI providers (connect/disconnect API keys) without leaving the dashboard. These panels provide that functionality via the ClawNow.ai-inspired slide-out and modal patterns.
Output: CreditsPanel, SettingsModal, ProviderCard components rendered as overlays from the dashboard layout.
</objective>

<execution_context>
@/Users/divykairoth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/divykairoth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-dashboard-redesign/20-01-SUMMARY.md
@lib/dashboard-store.ts
@app/(product)/dashboard/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CreditsPanel slide-out component</name>
  <files>components/dashboard/CreditsPanel.tsx</files>
  <action>
Create a slide-out panel that appears from the left side, overlaying the sidebar area. Uses Framer Motion for the slide animation.

**Structure:**
- Outer: Fixed overlay with bg-black/50 backdrop, onClick on backdrop calls `toggleCredits()` to close
- Panel: `motion.div` with initial={{ x: -320 }} animate={{ x: 0 }} exit={{ x: -320 }} transition={{ type: 'spring', damping: 25, stiffness: 300 }}. Width: w-80. Full height. bg-zinc-900. border-r border-zinc-800. z-50. overflow-y-auto.
- Wrap in `AnimatePresence` in the parent (layout.tsx) for exit animation.

**Panel content (top to bottom):**
1. Header: "Credits" title in text-lg font-semibold, X close button (lucide `X` icon) aligned right
2. Large balance display: `$X.XX` in text-5xl font-bold text-white, centered. Below: "REMAINING CREDITS" in text-xs text-zinc-500 uppercase tracking-widest
3. Spend breakdown: Two rows side by side in a grid-cols-2:
   - "$0.00 today" (text-sm text-zinc-400, label "Daily spend" in text-xs text-zinc-500)
   - "$0.00 this month" (same styling, label "Monthly spend")
4. Divider: border-t border-zinc-800 my-4
5. Top-up section:
   - Label: "Add credits" in text-sm text-zinc-300
   - Input row: `$` prefix + number input (type="number", min=1, step=0.01, default 10) with bg-zinc-800 border border-zinc-700 rounded-lg styling + "Top up →" button (bg-orange-500 hover:bg-orange-400 text-white rounded-lg px-4 py-2 text-sm font-medium)
   - On click: console.log('Top up:', amount) — actual Razorpay integration deferred to wiring phase
6. Auto-reload toggle:
   - Row with toggle switch (custom div-based toggle, bg-zinc-700 when off, bg-orange-500 when on, with white circle)
   - Label: "Automatically add $10 when credits drop below $2" in text-sm text-zinc-400
   - Store toggle state in local component state (not in Zustand — this will be a user preference from DB later)
7. Footer link: "Invoices & payment methods" in text-sm text-orange-500 hover:text-orange-400, links to `/dashboard/billing`

Read credits from `useDashboardStore` state: `credits.balance`, `credits.dailySpend`, `credits.monthlySpend`.
  </action>
  <verify>TypeScript compiles. Component renders when creditsOpen is true in store. Clicking backdrop closes it. Top-up button logs to console.</verify>
  <done>Credits panel slides out from left with balance display ($1.99), daily/monthly spend, top-up input with button, auto-reload toggle, and invoices link. Opens/closes smoothly with spring animation.</done>
</task>

<task type="auto">
  <name>Task 2: Build SettingsModal with provider management and wire overlays into layout</name>
  <files>
    components/dashboard/ProviderCard.tsx
    components/dashboard/SettingsModal.tsx
    app/(product)/dashboard/layout.tsx
  </files>
  <action>
**ProviderCard.tsx** — Individual AI provider card:
- Props: `{ name: string, description: string, icon: string (emoji or letter), connected: boolean, onConnect: () => void, onEdit: () => void }`
- Card: bg-zinc-800 border border-zinc-700 rounded-xl p-4 hover:border-zinc-600 transition
- Top: Icon in a w-10 h-10 rounded-lg bg-zinc-700 flex items-center justify-center, next to it the provider name in font-medium and description in text-sm text-zinc-400
- Bottom: If not connected — "Connect" button (border border-zinc-600 text-zinc-300 hover:bg-zinc-700 rounded-lg px-3 py-1.5 text-sm). If connected — green checkmark badge (bg-green-500/20 text-green-400 rounded-full px-2 py-0.5 text-xs "Connected") + "Edit" button (text-zinc-400 hover:text-white text-sm)
- OnConnect/OnEdit: For now, console.log — actual API key management deferred

**SettingsModal.tsx** — Full overlay modal:
- Outer: Fixed inset-0, bg-black/60 backdrop, z-50. Click backdrop calls `closeSettings()`
- Inner: `motion.div` with initial={{ opacity: 0, scale: 0.95 }} animate={{ opacity: 1, scale: 1 }} exit={{ opacity: 0, scale: 0.95 }}. Centered in viewport. bg-zinc-900 border border-zinc-800 rounded-2xl. Width: max-w-3xl w-full mx-4. Max height: max-h-[85vh] overflow-y-auto.
- Header: "Settings" title, X close button
- Tab bar: Horizontal tabs — Models, Providers, Channels, Skills. Active tab: text-orange-500 border-b-2 border-orange-500. Inactive: text-zinc-400 hover:text-white. Store active tab in Zustand `settingsTab`.
- **Providers tab content (default):** 2x3 grid of ProviderCard components with these providers:
  1. Anthropic (Claude) — "Advanced reasoning and analysis" — icon: "A"
  2. Google (Gemini) — "Multimodal AI by Google" — icon: "G"
  3. MiniMax — "Efficient language models" — icon: "M"
  4. OpenAI (GPT) — "Industry-leading language AI" — icon: "O"
  5. OpenRouter — "Unified API for multiple models" — icon: "R"
  6. Z.AI — "Next-gen AI platform" — icon: "Z"
  - Default: none connected (all show "Connect" button)
  - Store connected providers in local component state as `Set<string>` — toggling connect/disconnect updates the set. This is UI-only for now; actual API key storage comes in a later phase.
- **Models tab:** Placeholder text "Model configuration coming soon" centered in text-zinc-500
- **Channels tab:** Placeholder text "Channel management coming soon"
- **Skills tab:** Placeholder text "Skills configuration coming soon"

**layout.tsx** — Add overlay rendering:
- Import CreditsPanel and SettingsModal
- Import AnimatePresence from framer-motion
- After the main content div (sibling to sidebar and main), add:
  ```
  <AnimatePresence>
    {creditsOpen && <CreditsPanel />}
  </AnimatePresence>
  <AnimatePresence>
    {settingsOpen && <SettingsModal />}
  </AnimatePresence>
  ```
- Read `creditsOpen` and `settingsOpen` from `useDashboardStore`
- Keep all existing layout structure from Plan 01 intact — only ADD the overlay components
  </action>
  <verify>
Run `npm run build` — compiles without errors. In browser at `/dashboard`:
1. Click credits row in sidebar → Credits panel slides out from left with balance, spend, top-up
2. Click backdrop or X → panel closes with animation
3. Click settings row in sidebar → Settings modal appears centered
4. Click through tabs (Models, Providers, Channels, Skills) → content changes
5. Providers tab shows 2x3 grid of provider cards with Connect buttons
6. Click Connect on a provider → shows green "Connected" badge + Edit button
7. Click backdrop or X → modal closes
  </verify>
  <done>
Credits panel slides out with full balance display, top-up input, auto-reload toggle, and invoices link. Settings modal opens with tab navigation — Providers tab shows 2x3 grid of AI provider cards with connect/disconnect functionality. Both overlays animate smoothly and close on backdrop click.
  </done>
</task>

</tasks>

<verification>
1. Credits panel opens from sidebar click and shows balance, daily/monthly spend
2. Top-up input accepts dollar amount with "Top up" button
3. Auto-reload toggle switches on/off
4. Settings modal opens with 4 tabs
5. Providers tab shows 6 AI provider cards in 2x3 grid
6. Connect/disconnect toggles work on provider cards
7. All overlays have backdrop click to close
8. Framer Motion animations work (slide for credits, scale for settings)
9. No TypeScript compilation errors
</verification>

<success_criteria>
- Credits slide-out panel fully functional with balance display and top-up UI
- Settings modal renders with tab navigation and provider grid
- Provider cards toggle between connected/disconnected states
- Both overlays render as portal-like elements from the dashboard layout
- Build compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/20-dashboard-redesign/20-03-SUMMARY.md`
</output>
