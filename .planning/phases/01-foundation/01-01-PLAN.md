---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - postcss.config.mjs
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/lib/utils.ts
  - components.json
  - tailwind.config.ts
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "Next.js 15 app runs locally with `npm run dev` and no errors"
    - "`localhost:3000` displays default page"
    - "shadcn/ui Button component renders correctly"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies including next, react, tailwindcss, shadcn deps"
      contains: '"next"'
    - path: "src/app/layout.tsx"
      provides: "Root layout with html and body structure"
      min_lines: 10
    - path: "src/app/globals.css"
      provides: "Tailwind v4 imports and base styles"
      contains: '@import "tailwindcss"'
    - path: "components.json"
      provides: "shadcn/ui configuration"
      contains: '"style": "new-york"'
    - path: "src/lib/utils.ts"
      provides: "cn() utility function"
      contains: "clsx"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
    - from: "src/lib/utils.ts"
      to: "clsx + tailwind-merge"
      via: "npm imports"
      pattern: "from.*clsx"
---

<objective>
Create Next.js 15 project with TypeScript, Tailwind CSS v4, and shadcn/ui foundation.

Purpose: Establish the base project structure that all subsequent development builds upon.
Output: Working Next.js app with shadcn/ui initialized, ready for animation providers and design tokens.
</objective>

<execution_context>
@/Users/divykairoth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/divykairoth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project with TypeScript and Tailwind v4</name>
  <files>
    package.json
    tsconfig.json
    postcss.config.mjs
    src/app/layout.tsx
    src/app/page.tsx
    tailwind.config.ts
  </files>
  <action>
    Run `npx create-next-app@15 . --typescript --tailwind --eslint --app --src-dir --yes` in the project root.

    This creates:
    - Next.js 15 with App Router
    - TypeScript configuration
    - Tailwind CSS v4 (current default with create-next-app@15)
    - ESLint setup
    - src/ directory structure

    After creation, verify the project structure includes:
    - src/app/layout.tsx (root layout)
    - src/app/page.tsx (home page)
    - postcss.config.mjs (PostCSS for Tailwind)
    - package.json with next, react, typescript dependencies

    If Tailwind v3 is installed instead of v4, upgrade:
    ```bash
    npm install tailwindcss@4 @tailwindcss/postcss@4
    ```

    Update postcss.config.mjs to use @tailwindcss/postcss if needed:
    ```javascript
    const config = {
      plugins: {
        "@tailwindcss/postcss": {},
      },
    };
    export default config;
    ```

    Run `npm run dev` to verify no errors on initial load.
  </action>
  <verify>
    Run `npm run dev` and verify:
    - No build errors in terminal
    - Page loads at localhost:3000
    - Tailwind utilities work (check default styles render)
  </verify>
  <done>
    Next.js 15 project running locally with Tailwind v4 and TypeScript, no errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui with new-york style</name>
  <files>
    components.json
    src/lib/utils.ts
    src/app/globals.css
    src/components/ui/button.tsx
  </files>
  <action>
    Run `npx shadcn@latest init` with these options:
    - Style: new-york
    - Base color: neutral
    - CSS variables: yes

    This creates:
    - components.json with shadcn configuration
    - src/lib/utils.ts with cn() helper
    - Updates globals.css with CSS variables

    After init, add a test Button component:
    ```bash
    npx shadcn@latest add button
    ```

    Update src/app/page.tsx to import and render the Button:
    ```tsx
    import { Button } from "@/components/ui/button"

    export default function Home() {
      return (
        <main className="flex min-h-screen flex-col items-center justify-center p-24">
          <h1 className="text-4xl font-bold mb-8">BrewClaw</h1>
          <Button>Get Started</Button>
        </main>
      )
    }
    ```

    Verify shadcn/ui is using OKLCH color format in globals.css (not HSL) for Tailwind v4 compatibility.

    If globals.css has HSL format, the shadcn init version is outdated. Colors should look like:
    ```css
    --background: oklch(0.09 0 0);
    --foreground: oklch(1 0 0);
    ```
  </action>
  <verify>
    - components.json exists with "style": "new-york"
    - src/lib/utils.ts exists with cn() function
    - src/components/ui/button.tsx exists
    - Button renders correctly on the page with proper styling
    - `npm run dev` shows no errors
  </verify>
  <done>
    shadcn/ui initialized with new-york style. Button component renders with proper styling. cn() utility available.
  </done>
</task>

</tasks>

<verification>
Run these checks after all tasks complete:

1. `npm run dev` - no errors, page loads
2. Visit localhost:3000 - see "BrewClaw" heading and styled Button
3. Inspect Button element - verify Tailwind classes applied
4. Check components.json - style is "new-york"
5. Check globals.css - contains OKLCH colors and @import "tailwindcss"
</verification>

<success_criteria>
- Next.js 15 development server runs without errors
- shadcn/ui Button component displays with correct styling
- Project structure follows src/app/ convention
- Tailwind v4 CSS-first configuration is active
- All dependencies installed and working
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
